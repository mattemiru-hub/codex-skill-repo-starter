# Examples

Use this folder to store example prompts, source artifacts, or before-after notes for `codex-skill-repo-starter`.

Good example scenarios for this repo:

- split a mixed repo so bootstrap logic moves into its own starter repo
- polish an existing skill repo before publishing it to GitHub
- scaffold a new domain repo from `scripts/init_standard_skill_repo.ps1`
