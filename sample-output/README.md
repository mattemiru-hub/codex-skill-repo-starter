# Sample Output

Describe what a good final output from `codex-skill-repo-starter` should look like.

A strong final output usually includes:

- a clean repo tree with required files present
- a `SKILL.md` that clearly explains when the skill should trigger
- a README that explains install and usage without guessing
- references that support clarification, domain rules, prompt reuse, and QA
- a short note showing whether the repo is a domain repo or a starter repo
